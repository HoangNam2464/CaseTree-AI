/** Student — Reflection page (Branching Study post-outcome). Full implementation: feature/reflection */
export function ReflectionPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold text-gray-900 mb-4">Student Reflection</h2>
      <p className="text-sm text-gray-500">
        Guided reflection after reaching a case outcome — implement in <code>feature/reflection</code>.
      </p>
    </div>
  );
}
