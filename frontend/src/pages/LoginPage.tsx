/** CaseTree AI — Login Page. Full implementation: feature/auth */
export function LoginPage() {
  return (
    <div className="card p-8">
      <h2 className="text-xl font-semibold text-gray-900 mb-6">Sign in to CaseTree AI</h2>
      <p className="text-sm text-gray-500">
        Login form — to be implemented in <code>feature/auth</code>.
      </p>
    </div>
  );
}
