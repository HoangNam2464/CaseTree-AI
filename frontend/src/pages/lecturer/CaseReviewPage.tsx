/** Lecturer — Case review, edit, and publication page. Full implementation: feature/case-review */
export function CaseReviewPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold text-gray-900 mb-4">Review Case</h2>
      <p className="text-sm text-gray-500">
        Decision tree review and publish flow — implement in <code>feature/case-review</code>.
      </p>
    </div>
  );
}
