/** Lecturer — Cases list and generation page. Full implementation: feature/cases */
export function CasesPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold text-gray-900 mb-4">Case Studies</h2>
      <p className="text-sm text-gray-500">Scaffolded — implement in <code>feature/case-generation</code>.</p>
    </div>
  );
}
