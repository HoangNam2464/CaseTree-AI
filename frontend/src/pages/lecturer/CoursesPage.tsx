/** Lecturer — Courses management page. Full implementation: feature/courses */
export function CoursesPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold text-gray-900 mb-4">My Courses</h2>
      <p className="text-sm text-gray-500">Scaffolded — implement in <code>feature/courses</code>.</p>
    </div>
  );
}
