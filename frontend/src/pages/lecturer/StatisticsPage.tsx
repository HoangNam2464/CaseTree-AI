/** Lecturer — Simple statistics page. Full implementation: feature/statistics */
export function StatisticsPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold text-gray-900 mb-4">Statistics</h2>
      <p className="text-sm text-gray-500">Branch selection and participation stats — implement in <code>feature/statistics</code>.</p>
    </div>
  );
}
